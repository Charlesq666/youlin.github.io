export type ExperienceType = {
  companyUrl: string;
  techImgs: string[];
  companylogo: string;
  title: string;
  company: string;
  location: string;
  from: string;
  to: string;
  responsibilities: string[];
}

export type ProjectType = {
  title: string;
  descriptions: string[];
  imgUrl: string;
}

export type SkillObj = {
  name: string;
  imgUrl: string;
}